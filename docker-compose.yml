version: '3'

services:
  postgres:
    image: 'postgres:10'
    container_name: 'java-postgres'
    environment:
      - 'POSTGRES_USER=root'
      - 'POSTGRES_HOST_AUTH_MET HOD=trust'
      - 'POSTGRES_DB=my_db'

  app:
    build: ./
    container_name: 'java-app'
    ports:
      - 8080:8080